This directory is the DEFAULT home for audio files and filter FV contents before filter analysis has occured. If filtering passes then the meaningful audio file is further FV extratced from here and then copied to the $CIRAINBOW/data (or $BASE_FOUND_DIR/data directory) for deployment.